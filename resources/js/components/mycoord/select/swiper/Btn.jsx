import React from 'react'

export function Btn(props) {
    {
        return (
            <div>
                <button type="button" onClick={props.onClick}>ボタン</button>
            </div>
        )
    }
}
